name: util
description: reusable scripts / programs
inputs:
  cmd:
    required: true
runs:
  using: composite
  steps:
    - run: |
        PATH=${{github.action_path}}:$PATH
        ${{inputs.cmd}}
